import { ProtoExntends } from '../../../common/helper'
import { BasePluginOptions } from '../../editor'

/**
 * 移动端工具栏
 */
export default class MobileToolbar {
  constructor (options: BasePluginOptions)
}
